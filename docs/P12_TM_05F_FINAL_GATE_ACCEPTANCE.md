# P12-TM-05F Final Gate Acceptance

**Date:** 2026-08-03  
**Reviewer:** Sol  
**Reviewed handoff:** `docs/handoffs/P12_TM_05F_HANDOFF.md`  
**Decision:** `ACCEPT`  
**Tasks closed:** `P12-TM-05`, `P12-TM-05F`  
**Downstream:** single-client `P12-DS` is `READY` for a new immutable artifact and clean DS-01 rerun

## 1. Decision

The repository delta closes P12-TM-05-R1 through R3 from
`docs/P12_TM_05_GATE_REVIEW.md`. The complete Factored Leap statement now has
the bounded three-line presentation frozen by P12-SOL-03, while one- and
two-line statements preserve their legacy two-line header geometry.

No new immutable artifact was generated by this review. The previously frozen
`2026-08-02-ce64b9b` artifact remains a failed historical batch and must not be
reused for the clean DS-01 rerun.

## 2. Closure of individual findings

### R1 — ACCEPT

`TheoremStatementGeometry` maps rendered line counts 1, 2 and 3 to effective
counts 2, 2 and 3. The corresponding widget heights are 22, 22 and 33 pixels,
and graph viewport offsets are 37, 37 and 48 pixels. Four or more lines fail
explicitly.

The real FS-06 client vector selected registered `mathmod:hop` and recorded:

```text
locale: en_us
window / GUI scale: 1024x800 / 2
availableWidth: 133
exactLineCount: 1
line width: 92
statement hitbox: 140x22
graph origin: 115
formula: push(self,(0,.35,0))
```

The retained screenshot
`run/client/screenshots/mathmod-fs-06-en_us-preview.png` visibly preserves the
legacy header gap and graph origin.

### R2 — ACCEPT

`FactoredLeapCatalogException.isExact` now requires both:

```text
mathmod:factored_leap
push(halve(look)+halve(up))
```

The behavioral test accepts that exact registered pair and rejects the same id
with a different formula and the same formula with a different id.
`ProgramPresets` and the frozen formulas remain unchanged.

### R3 — ACCEPT

The focused geometry test executes the effective counts, heights and viewport
offsets for one, two and three lines and the explicit fourth-line rejection.
FS-06 adds the real-font/runtime one-line oracle. FS-01 through FS-05 remain
present with distinct vector/locale filenames.

## 3. Repository and ownership review

The correction writes only files authorized by
`docs/P12_TM_05_GATE_REVIEW.md`:

```text
src/main/java/com/mathmod/client/screen/RuneProgrammerScreen.java
src/main/java/com/mathmod/client/UiPreviewHarness.java
src/main/java/com/mathmod/client/UiPreviewMatrix.java
src/test/java/com/mathmod/client/UiPreviewMatrixTest.java
src/test/java/com/mathmod/client/screen/FactoredLeapStatementPresentationTest.java
docs/UX_AUDIT.md
docs/handoffs/P12_TM_05F_HANDOFF.md
```

No change was found to `TheoremStatementPresentation`, `ProgramPresets`, either
formula or localization, `ProgramGraph`, `GuidedWorkspaceState`,
`ProgramSurfaceMode`, menus, networking, persistence, Data Components, schemas,
server behavior or public APIs. Unrelated pre-existing working-tree files are
not claimed by this gate.

## 4. Independent command reproduction

Sol reproduced the contractual focused command with
`GRADLE_USER_HOME=C:\codex-gradle-a0` and the correct class name
`com.mathmod.ServerSideIsolationTest`:

```text
ProgramPresetsTest:                         17
UiPreviewMatrixTest:                         8
ProgrammerLayoutTest:                       11
FactoredLeapStatementPresentationTest:       2
ServerSideIsolationTest:                     2
total:                                      40
failures/errors/skipped:                 0/0/0
```

Sol also reproduced:

```text
global JUnit: 533 tests, 0 failures, 0 errors, 0 skipped, 136 classes
build: BUILD SUCCESSFUL
git diff --check: clean, with line-ending warnings only
```

The P12-TM-05F handoff's `com.mathmod.server.ServerSideIsolationTest` spelling
and focused total of 38 are documentary inaccuracies. They are superseded by
the independently reproduced command and XML counts above and do not mask a
missing test or product defect.

## 5. Downstream authorization

The statement presentation gate is closed. `P12-DS` may return to `READY` for
the single-client sequence only, beginning with all of the following:

1. build a new immutable JAR from the accepted repository state;
2. record its commit, SHA-256 and a new batch identity;
3. restore a clean standalone server/client checkpoint;
4. rerun DS-01 in full before starting DS-02 or any later single-client row.

The prior screenshots or logs do not substitute for this clean rerun. DS-02
through DS-05 and DS-07 through DS-09 remain blocked on the new DS-01 result.
DS-06 remains `P12-DS-MP BACKLOG`; it is neither passed nor waived.
